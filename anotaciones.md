```js
// Funções de teste global - executada antes de qualquer teste
beforeAll(() => {
    // Criar novo user para usar nos outros testes


});

// Roda quando todos os testes forem concluidos
afterAll(() => {
    // Remover novo user para não poluir o banco de dados
});

// Roda uma vez antes de cada teste
beforeEach(() => {

});

// Roda uma vez após cada teste
afterEach(() => {

});


```